# AGENTS.md

Canonical AI-assistant and contributor guidance for Jan Server lives in
[CLAUDE.md](CLAUDE.md). Start there for the project overview, architecture
rules, code conventions, ports, and the before-committing checklist.

For deeper patterns and standards, see [docs/conventions/](docs/conventions/).

Frontend code lives in `apps/web` (React + Vite chat UI) and the shared
`packages/interfaces` component library (`@janhq/interfaces`).
