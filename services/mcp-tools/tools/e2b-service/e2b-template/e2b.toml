# This is a config for E2B sandbox template.
# You can use template ID (1gi80jewdp5qd2k3uqai) or template name (jan-browser-sandbox) to create a sandbox:

# Python SDK
# from e2b import Sandbox, AsyncSandbox
# sandbox = Sandbox.create("jan-browser-sandbox") # Sync sandbox
# sandbox = await AsyncSandbox.create("jan-browser-sandbox") # Async sandbox

# JS SDK
# import { Sandbox } from 'e2b'
# const sandbox = await Sandbox.create('jan-browser-sandbox')

memory_mb = 8_192
cpu_count = 8
dockerfile = "e2b.Dockerfile"
template_name = "jan-browser-sandbox"
